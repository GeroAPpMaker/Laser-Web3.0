# Laserv3

Vue 3 + Vite starter with Vue Router, Tailwind CSS, and Supabase.

## Setup

```sh
npm install
```

Copy `.env.example` to `.env` and set:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

```sh
npm run dev
```
# Laser-Web3.0
